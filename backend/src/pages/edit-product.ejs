<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>
            <% if(editing){ %>Edit Product<% } else { %>Add Product<% } %>
        </title>
    </head>
    <body>
        <h1><% if(editing){ %>Edit Product<% } else { %>Add Product<% } %></h1>

        <form method="POST" action="/<% if(editing){ %>edit-product<% } else { %>add-product<% } %>">
            <div>
                <label>Name</label>
                <input type="text" name="name" value="<% if(editing){ %><%= product.name %><% } %>" required />
            </div>
            <div>
                <label>Image_link</label>
                <input type="text" name="image_link" value="<% if(editing){ %><%= product.image_link %><% } %>" required />
            </div>
            <div>
                <label>Description</label>
                <input type="text" name="description" value="<% if(editing){ %><%= product.description %><% } %>"  required />
            </div>
            <div>
                <label>Brand</label>
                <input type="text" name="brand" value="<% if(editing){ %><%= product.brand %><% } %>" required />
            </div>
            <div>
                <label>Product type</label>
                <input type="text" name="product_type" value="<% if(editing){ %><%= product.product_type %><% } %>" required />
            </div>
            <% if(editing){ %>
                <div>
                    <input type="hidden" name="productId" value="<%= product.id %>" />
                </div>
                <% } %>
                <div>
                    <button type="submit"><% if(editing){ %>Edit Product<% } else { %>Add Product<% } %></button>
                </div>
        </form>
    </body>
</html>